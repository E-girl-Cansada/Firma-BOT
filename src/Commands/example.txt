const Command = require('../../Structures/Command.js');

module.exports = class extends Command {

	constructor(...args) {

		super(...args, {

			aliases: [''],
			description: [''],
			usage: [''],
			category: [''],

		});
	}

	async run(message) {

		message.channel.send('');

	}

};